version: "3.8"
services:
  web:
    image: consomme:latest
    ports:
      - "3000:3000"
  redis:
    image: "redis:alpine"
  postgres:
    restart: always
    image: postgres:latest
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
